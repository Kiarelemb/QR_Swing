# 控件不透明状态梳理

本文档梳理当前项目中基础控件、组合控件与窗口背景图相关的 `setOpaque(boolean)` 使用现状。目标是先记录事实，方便后续统一规则、修复背景图场景下控件遮挡或透明过度的问题。

## 当前总体机制

### 背景图入口

- `QRFrame#setBackgroundImage(String filePath)` 是主窗体背景图的核心入口。
- 背景图设置成功后：
  - `QRBorderContentPanel#setImage(...)` 保存背景图。
  - `QRSwing.windowImageSet = true`。
  - 调用 `QRComponentUtils.componentLoopToSetOpaque(this.contentPane, false)`。
  - 也就是说，窗口内容树中大部分 `JComponent` 会被递归设为透明。

### 递归透明方法

位置：`src/swing/qr/kiarelemb/utils/QRComponentUtils.java`

```java
public static void componentLoopToSetOpaque(JComponent com, boolean opaque)
```

当前行为：

- 从传入组件的子组件开始遍历，不直接处理传入的 `com` 本身。
- 对所有遍历到的 `JComponent` 执行 `setOpaque(opaque)`。
- 遇到 `QRPanel` 会继续递归其子组件。
- 遇到非 `QRPanel` 的容器型组件时，只会设置该组件自身，不会继续向下递归。

影响：

- `QRPanel` 体系内的组件会被深度处理。
- `JScrollPane`、`JViewport`、`JComboBox`、`JSpinner` 等非 `QRPanel` 容器内部子组件不一定被递归处理。
- 如果某个组件重写了 `setOpaque`，递归设置可能无效。例如 `QRButton`、`QRSliderEnhancement` 会强制保持透明。

### 对话框相关

- `QREmptyDialog` 打开时默认调用 `componentLoopToSetOpaque(contentPane, false)`，可通过 `setComponentsOpaqueDefault()` 关闭该行为。
- `QRDialog` 有同类字段 `toSetOpaque` 和 `setComponentsOpaqueDefault()`，但打开窗口时递归设置透明的代码目前被注释掉了。因此当前 `QRDialog` 不会自动按该字段批量调整 opaque。

## 基础控件现状

| 控件 | 当前 opaque 策略 | 背景色刷新 | 备注 |
|---|---|---|---|
| `QRPanel` | 默认构造 `setOpaque(false)`；`QRPanel(boolean)` 可指定；`QRPanel(LayoutManager)` 未显式设 opaque，沿用 `JPanel` 默认值 | `FRAME_COLOR_BACK` | 这是最大的不一致来源之一：不同构造器默认 opaque 不一致。 |
| `QRButton` | 构造时 `setOpaque(false)`，并重写 `setOpaque(boolean)` 强制 `false` | `FRAME_COLOR_BACK` | 始终透明，自绘 hover/pressed 背景；背景图开启时 hover 透明度会降低。 |
| `QRRoundButton` | 继承 `QRButton`，强制透明 | 继承 `QRButton` | 圆角按钮背景由自绘逻辑负责。 |
| `QRMenuItem` | 继承 `QRButton`，强制透明 | 继承并扩展 `QRButton` | 菜单项透明策略来自按钮。 |
| `QRCheckBox` | 构造时 `setOpaque(false)` | `FRAME_COLOR_BACK` | 始终倾向透明，未重写 `setOpaque`，可被外部改掉。 |
| `QRRadioButton` | 未显式设置 opaque | `FRAME_COLOR_BACK` | 依赖 `JRadioButton` 默认值，背景图递归时会被改为透明。 |
| `QRSlider` | 构造时 `setOpaque(false)` | `FRAME_COLOR_BACK` | 未重写 `setOpaque`，可被外部改掉。 |
| `QRSliderEnhancement` | 构造时 `setOpaque(false)`，并重写 `setOpaque(boolean)` 强制 `false`；内部 `Slide` 也强制透明 | 无明显背景刷新 | 与 `QRButton` 类似，外部无法改成 opaque。 |
| `QRSplitPane` | 构造时 `setOpaque(false)` | `componentFresh()` 中背景刷新被注释 | 倾向透明；子组件刷新靠遍历。 |
| `QRScrollBar` | 构造时 `setOpaque(false)` | `FRAME_COLOR_BACK` | 滚动条自身透明，UI 负责轨道和滑块绘制。 |
| `QRScrollPane` | 构造时 `setOpaque(false)` | `FRAME_COLOR_BACK` | `JViewport` opaque 跟随 view：`viewport.setOpaque(view == null || view.isOpaque())`。这是比较明确的一套局部规则。 |
| `QRInternalScrollPane` | 构造时 `setOpaque(false)` | 继承 `QRPanel` | 内部滚动面板倾向透明。 |
| `QRTable` | 构造和 `componentFresh()` 都 `setOpaque(true)`；表头也 `setOpaque(true)` | `FRAME_COLOR_BACK`，表头 `TEXT_COLOR_BACK` | 明确要求实底。即使背景图递归设为 false，后续 `componentFresh()` 会恢复 true。 |
| `QRList` | 未显式设置 opaque | `TEXT_COLOR_BACK` | 依赖 `JList` 默认值；背景图递归时会被改为透明。 |
| `QRTree` | 构造时 `setOpaque(false)`；Renderer 构造时 `setOpaque(false)` | 树本体 `FRAME_COLOR_BACK`；Renderer 刷新时设置非选中/选中背景色 | 树本体倾向透明，但 Renderer 刷新时会设置背景色。 |
| `QRTextField` | 未显式设置 opaque | `FRAME_COLOR_BACK` | 依赖 `JTextField` 默认值；背景图递归会改为透明。 |
| `QRTextArea` | 未显式设置 opaque | `TEXT_COLOR_BACK` | 依赖 `JTextArea` 默认值；背景图递归会改为透明。 |
| `QREditorPane` | 未显式设置 opaque | `TEXT_COLOR_BACK` | 依赖 `JEditorPane` 默认值；背景图递归会改为透明。 |
| `QRTextPane` | 构造时 `setOpaque(!QRSwing.windowImageSet)` | `TEXT_COLOR_BACK` | 构造时根据全局背景图状态决定；后续全局状态变化时不自动同步，除非递归设置或手动设置。 |
| `QRComboBox` | 未显式设置 opaque | `FRAME_COLOR_BACK`，并刷新自定义 UI | 依赖 `JComboBox` 默认值；内部 editor/button/popup/scrollPane 由 UI 刷新，但 opaque 未统一。 |
| `QRSpinner` | 未显式设置 opaque | 本体、editor、内部文本框都设 `FRAME_COLOR_BACK` | 依赖 `JSpinner` 默认值；内部 editor opaque 未统一处理。 |
| `QRLabel` | 未显式设置 opaque | `FRAME_COLOR_BACK` | `JLabel` 默认通常是非 opaque；但没有显式规则。 |
| `QRProgressBar` | 继承 `QRPanel`，默认透明 | 继承 `QRPanel` | 自绘进度。 |

## 组合控件现状

| 控件 | 当前 opaque 策略 | 备注 |
|---|---|---|
| `QRBorderContentPanel` | 继承 `QRPanel`，构造 `super(false)` | 背景图实际绘制在此组件的 `paint(Graphics)` 中。 |
| `QRContractiblePanel` | 继承 `QRPanel` | 默认取决于调用的 `QRPanel` 构造器；内部 `QRColumnPanel`、`QRColumnContentPanel` 也继承 `QRPanel`。 |
| `QRListTabbedPane` | 继承 `QRPanel` | 默认透明。 |
| `QRMenuPanel` | 继承 `QRPanel` | 默认透明。 |
| `QRMenuButton` | 继承 `QRButton` | 强制透明；弹出菜单相关组件另行刷新。 |
| `QRMenuButtonOriginal` | 继承 `QRButton` | 强制透明。 |
| `QRPopupItem` | 继承 `QRButton` | 强制透明。 |
| `QRPopupMenu` | 继承 `QREmptyDialog` | 打开时默认会把 `contentPane` 子树设为透明；内部 `PopupMainPanel` 继承 `QRPanel`。 |
| `QRStatePanel` | 继承 `QRPanel` | 默认透明；内部分隔标签继承 `QRLabel`。 |
| `QRTabbedContentPanel` | 继承 `QRPanel` | 默认透明。 |
| `QRTabbedPane` | 继承 `QRPanel` | 默认透明；内部 tab panel 继承 `QRPanel`；关闭按钮是普通 `JButton`，构造时 `setOpaque(false)`。 |
| `QRTransparentSplitPane` | 继承 `QRPanel` | 默认透明；内部 divider 继承 `QRPanel`。 |
| `QRTreeTabbedPane` | 继承 `QRPanel` | 默认透明；绑定的实际页面 panel 由外部传入，opaque 不由本类统一。 |

## 辅助控件与渲染器

| 控件 | 当前 opaque 策略 | 备注 |
|---|---|---|
| `QRTableCellRenderer` | 构造 `setOpaque(true)` | 表格单元格渲染必须实底，否则选择/普通背景显示不稳定。 |
| `QRTableHeaderRenderer` | 构造 `setOpaque(true)` | 表头实底。 |
| `QRToolTip` | 未显式设置 opaque | 刷新背景为 `FRAME_COLOR_BACK`，并自绘圆角。 |
| `QRImagePanel` | 构造 `setOpaque(false)` | 用于图片绘制，倾向透明。 |
| `QRPicturePreviewPanel` | 构造 `setOpaque(true)` | 图片预览面板明确实底。 |
| `QRRGBColorPane` | 继承 `QRPanel` 默认透明；颜色预览标签 `showColorLabel.setOpaque(true)` | 颜色块必须实底。 |
| `QRClearableTextField` | 继承 `QRPanel` 默认透明 | 内部文本框继承 `QRTextField`，清除按钮继承 `QRCloseButton`/`QRButton`。 |
| `QRMessageLabel` | 继承 `QRLabel` | 未显式设置 opaque，自绘消息背景。 |

## 当前主要问题点

1. `QRPanel` 构造器默认值不一致。
   - `new QRPanel()` 是透明。
   - `new QRPanel(layout)` 没有调用 `setOpaque(false)`，因此可能保持 `JPanel` 默认 opaque。
   - 组合控件大量继承 `QRPanel`，因此构造器选择会直接影响透明策略。

2. 全局递归设置过于粗暴。
   - 背景图开启后直接对内容树执行 `setOpaque(false)`。
   - 这会影响文本框、列表、树、滚动面板、表格等控件的可读性。
   - 但部分控件又通过重写或 `componentFresh()` 抵消该设置，导致状态更加难预测。

3. 非 `QRPanel` 容器的子组件不一定被递归处理。
   - 例如 `JScrollPane` 的 `JViewport`、`JComboBox` 的 popup/editor、`JSpinner` 的 editor。
   - `QRScrollPane` 自己补了一套 viewport 规则，但其他复杂控件没有类似约定。

4. `componentFresh()` 会改变背景色，但大多数控件不处理 opaque。
   - 主题刷新和背景图透明策略是两条线。
   - 刷新主题后，有些控件只是改背景色，opaque 仍保持之前状态。
   - `QRTable` 是少数会在刷新时恢复 `setOpaque(true)` 的控件。

5. `QRTextPane` 构造时读取 `QRSwing.windowImageSet`。
   - 如果控件创建时背景图未启用，它默认 opaque。
   - 后续再开启背景图，只能依赖递归设置覆盖。
   - 如果后续关闭背景图，目前没有对称的恢复逻辑。

6. `QRDialog` 与 `QREmptyDialog` 行为不一致。
   - `QREmptyDialog` 打开时默认递归透明。
   - `QRDialog` 相关代码被注释，字段和方法仍保留，实际不生效。

## 建议后续整理方向

1. 先定义组件分类，而不是到处手动 `setOpaque`。
   - 容器类：默认透明，例如普通 `QRPanel`、组合面板、布局面板。
   - 输入/阅读类：默认实底，例如 `QRTextField`、`QRTextArea`、`QRTextPane`、`QRList`、`QRTable`。
   - 自绘类：由自绘逻辑负责，例如 `QRButton`、`QRSliderEnhancement`、`QRProgressBar`。
   - 视觉预览类：按用途固定，例如颜色块、图片预览保持实底。

2. 将“背景图适配”从递归粗改改成组件级策略。
   - 可以考虑新增接口，例如 `QRBackgroundAware` 或 `QROpaquePolicy`。
   - 每个控件声明自己在普通模式和背景图模式下应该 opaque 还是 transparent。
   - 递归方法只调用策略，不直接盲目 `setOpaque(false)`。

3. 统一 `QRPanel` 构造器。
   - 建议所有构造器都显式设置 opaque。
   - 如果要保留 `QRPanel(LayoutManager)` 的现有行为，需要在文档中标明它是实底/默认布局面板。

4. 给背景图关闭场景补恢复逻辑。
   - 当前 `setBackgroundImage(null)` 或图片不可用时只清空图，不会递归恢复 opaque。
   - 后续应明确是否恢复普通界面策略。

5. 对滚动类、输入类做重点修复。
   - `QRScrollPane`、`JViewport`、`QRTextPane`、`QRTextArea`、`QRList`、`QRTable` 是最容易出现“背景图透出但文字不可读”或“空白区域颜色不一致”的区域。

## 初步结论

当前项目的 opaque 状态不是单纯“乱设”，而是混合了三套逻辑：

- 控件自身默认策略。
- 背景图开启时的全局递归透明策略。
- 主题刷新时部分控件自行恢复背景或 opaque 的策略。

后续如果要修，建议不要直接继续增删零散 `setOpaque`，而是先确定每类控件在“普通窗口”和“背景图窗口”下的期望策略，再把递归透明改成按策略应用。
